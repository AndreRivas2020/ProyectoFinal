@model List<MvcMusicStore.Models.Album>
@{
ViewBag.Title = "Tienda AR";
}
<div id="promotion">
</div>
<h3><em>Recien</em> salido</h3>
<section class="py-0">
            <div class="container px-4 px-lg-5 mt-5">
                <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">
@*<ul id="album-list">*@
@foreach (var album in Model)
{
@*<li><a href="@Url.Action("Details", "Store",
new { id = album.AlbumId })">
<img alt="@album.Title" src="@album.AlbumArtUrl"
/> <span>@album.Title</span> </a>
</li>*@   <a href="@Url.Action("Details", "Store", new { id = album.AlbumId })">            <div class="col mb-0" >
                        <div class="card h-25">
                            <!-- Sale badge-->
                            <div class="badge bg-dark text-white position-absolute" style="top: 0.5rem; right: 0.5rem">Sale</div>
                            <!-- Product image-->
                            <img class="card-img-top" alt="@album.Title" src="@album.AlbumArtUrl" />
                            <!-- Product details-->
                            <div class="card-body p-4">
                                <div class="text-center">
                                    <!-- Product name-->
                                    <h5 class="fw-bolder">@album.Title</h5> 
                                </div>
                            </div>
                        </div>
                    </div>
        </a>
}
                    </div>
            </div>
        </section>
 @*   </ul> *@